// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.7.4
// Generated from: nitro_type_coverage.native.dart
//
// Abstract C++ interface for NitroTypeCoverage.
// Mirrors RN Nitro's HybridXxxSpec pattern — pure-virtual C++ with stdlib types.
// 1. Subclass HybridNitroTypeCoverage and implement all pure-virtual methods.
// 2. Register via nitro_type_coverage_register_factory() (multi-instance) or
//    nitro_type_coverage_register_impl() (single-instance legacy API).
#pragma once
#ifndef NITRO_TYPE_COVERAGE_NATIVE_G_H
#define NITRO_TYPE_COVERAGE_NATIVE_G_H

#include <stdint.h>
#include <stddef.h>
#include <cstring>
#include <functional>
#include <memory>
#include <optional>
#include <string>
#include <stdexcept>
#include <vector>
#include <variant>
#include "nitro_type_coverage.bridge.g.h"

#if defined(_MSC_VER) && !defined(strdup)
#define strdup _strdup
#endif

/// Lightweight read-only view over a binary payload.
/// For @zeroCopy returns: return a malloc'd buffer — ownership transfers,
/// and the bridge frees it when Dart's view is GC'd (never a member/stack ptr).
struct NitroCppBuffer {
    const uint8_t* data;
    size_t size;
};

/// Binary encoder for @HybridRecord / @NitroVariant return values.
/// Mirrors RN Nitro's NitroRecordWriter — build then call toNative().
struct NitroRecordWriter {
    std::vector<uint8_t> _buf;
    NitroRecordWriter() { _buf.reserve(64); }
    void writeInt(int64_t v) { _buf.insert(_buf.end(), reinterpret_cast<const uint8_t*>(&v), reinterpret_cast<const uint8_t*>(&v) + 8); }
    void writeInt32(int32_t v) { _buf.insert(_buf.end(), reinterpret_cast<const uint8_t*>(&v), reinterpret_cast<const uint8_t*>(&v) + 4); }
    void writeInt8(int8_t v) { _buf.push_back(static_cast<uint8_t>(v)); }
    void writeDouble(double v) { _buf.insert(_buf.end(), reinterpret_cast<const uint8_t*>(&v), reinterpret_cast<const uint8_t*>(&v) + 8); }
    void writeBool(bool v) { _buf.push_back(v ? 1 : 0); }
    void writeString(const std::string& s) { int32_t n = (int32_t)s.size(); writeInt32(n); _buf.insert(_buf.end(), s.begin(), s.end()); }
    void writeBytes(const uint8_t* data, size_t n) { if (data && n) _buf.insert(_buf.end(), data, data + n); }
    /// Returns heap-allocated [4B length][payload]. Caller must ::free().
    uint8_t* toNative() const {
        int32_t payloadLen = (int32_t)_buf.size();
        uint8_t* out = (uint8_t*)::malloc(sizeof(int32_t) + (size_t)payloadLen);
        if (!out) return nullptr;
        ::memcpy(out, &payloadLen, sizeof(int32_t));
        if (payloadLen > 0) ::memcpy(out + sizeof(int32_t), _buf.data(), (size_t)payloadLen);
        return out;
    }
    /// Non-owning view of the payload (no length prefix). Valid while this writer lives.
    /// For encoding NESTED payloads only — never return it or pass it to an
    /// emit_* stream helper (both transfer ownership; use toNativeBuffer()).
    NitroCppBuffer toBuffer() const { return { _buf.data(), _buf.size() }; }
    /// Heap-allocated [4B length][payload] wrapped in a buffer whose size
    /// includes the prefix. Use as the return value of record/variant methods
    /// AND as the item passed to emit_* stream helpers: ownership transfers
    /// to Dart (freed after decoding via the <lib>_nitro_free export).
    NitroCppBuffer toNativeBuffer() const { return { toNative(), sizeof(int32_t) + _buf.size() }; }
    /// Encodes a List<@HybridRecord> the way Dart decodes one:
    ///   [4B count][8B x count offsets][item bytes...]
    /// Offsets are payload-relative. Dart uses the table for O(1) access, so
    /// a plain [4B count][items] encoding is NOT interchangeable — the reader
    /// would consume item bytes as offsets. Primitive-list ARGUMENTS use this
    /// same shape; primitive-list RETURNS are plain (writeInt32(count) + items).
    template <typename T, typename WriteItem>
    void writeIndexedList(const std::vector<T>& items, WriteItem writeItem) {
        int32_t n = (int32_t)items.size();
        writeInt32(n);
        size_t tableStart = _buf.size();
        for (int32_t i = 0; i < n; i++) writeInt(0); // reserved, backpatched below
        std::vector<int64_t> offsets((size_t)n);
        for (int32_t i = 0; i < n; i++) {
            offsets[(size_t)i] = (int64_t)_buf.size();
            writeItem(*this, items[(size_t)i]);
        }
        for (int32_t i = 0; i < n; i++)
            ::memcpy(_buf.data() + tableStart + (size_t)i * 8, &offsets[(size_t)i], 8);
    }
};

// Bounds-checked binary reader for @HybridRecord wire payloads.
// Every readNullTag() bounds-checks before the byte access (IMPROVEMENTS.md §3.3).
struct NitroRecordReader {
  const uint8_t* _data;
  size_t         _size;
  size_t         _offset;
  explicit NitroRecordReader(NitroCppBuffer buf)
      : _data(buf.data), _size(buf.size), _offset(0) {}
  explicit NitroRecordReader(const uint8_t* data, size_t size)
      : _data(data), _size(size), _offset(0) {}
private:
  void _require(size_t n) const {
      if (_offset + n > _size)
          throw std::runtime_error("NitroRecordReader: buffer underflow");
  }
public:
  int64_t readInt() {
      _require(8); int64_t v; std::memcpy(&v, _data + _offset, 8); _offset += 8; return v;
  }
  int32_t readInt32() {
      _require(4); int32_t v; std::memcpy(&v, _data + _offset, 4); _offset += 4; return v;
  }
  int8_t readInt8() {
      _require(1); return static_cast<int8_t>(_data[_offset++]);
  }
  double readDouble() {
      _require(8); uint64_t b; std::memcpy(&b, _data + _offset, 8);
      _offset += 8; double v; std::memcpy(&v, &b, 8); return v;
  }
  bool readBool() {
      _require(1); return _data[_offset++] != 0;
  }
  std::string readString() {
      int32_t len = readInt32(); _require((size_t)len);
      std::string s(reinterpret_cast<const char*>(_data + _offset), (size_t)len);
      _offset += (size_t)len; return s;
  }
  // Bounds-checked null tag read — throws if no byte remains.
  bool readNullTag() {
      if (_offset + 1 > _size)
          throw std::runtime_error("NitroRecordReader: null tag read past end of buffer");
      return _data[_offset++] == 0;
  }
  // TypedData blob: [4B count][count bytes] → reads into caller-owned vector.
  void readBytes(uint8_t* dst, size_t n) {
      _require(n); std::memcpy(dst, _data + _offset, n); _offset += n;
  }
  // Repositions the cursor. Needed to follow an indexed list's offset table,
  // which is random-access rather than sequential.
  void seek(size_t offset) {
      if (offset > _size)
          throw std::runtime_error("NitroRecordReader: seek past end of buffer");
      _offset = offset;
  }
  size_t position() const { return _offset; }
  // Decodes [4B count][8B x count offsets][item bytes...] — the shape Dart
  // encodes a List<@HybridRecord> in, both as an argument and as a return.
  // readItem is called with this reader already positioned at each item, so a
  // per-item Record::fromReader(r) works directly. Do NOT hand a plain
  // [4B count][items] payload to this: the offsets would be item bytes.
  template <typename T, typename ReadItem>
  std::vector<T> readIndexedList(ReadItem readItem) {
      int32_t n = readInt32();
      if (n < 0) throw std::runtime_error("NitroRecordReader: negative list count");
      std::vector<int64_t> offsets((size_t)n);
      for (int32_t i = 0; i < n; i++) offsets[(size_t)i] = readInt();
      std::vector<T> out;
      out.reserve((size_t)n);
      for (int32_t i = 0; i < n; i++) {
          seek((size_t)offsets[(size_t)i]);
          out.push_back(readItem(*this));
      }
      return out;
  }
};
// @HybridStruct inline binary codecs (generated by Nitrogen).
inline TcPoint nitro_TcPoint_fromReader(NitroRecordReader& _r) {
    TcPoint _s{};
    _s.x = _r.readDouble();
    _s.y = _r.readDouble();
    _s.z = _r.readDouble();
    return _s;
}
inline void nitro_TcPoint_encodeInto(const TcPoint& _s, NitroRecordWriter& w) {
    w.writeDouble(_s.x);
    w.writeDouble(_s.y);
    w.writeDouble(_s.z);
}
inline TcOptScalars nitro_TcOptScalars_fromReader(NitroRecordReader& _r) {
    TcOptScalars _s{};
    _s.count = _r.readInt();
    _s.ratio = _r.readDouble();
    _s.flag = _r.readBool() ? 1 : 0;
    _s.keep = _r.readInt();
    return _s;
}
inline void nitro_TcOptScalars_encodeInto(const TcOptScalars& _s, NitroRecordWriter& w) {
    w.writeInt(_s.count);
    w.writeDouble(_s.ratio);
    w.writeBool(_s.flag != 0);
    w.writeInt(_s.keep);
}
inline TcRichStruct nitro_TcRichStruct_fromReader(NitroRecordReader& _r) {
    TcRichStruct _s{};
    { std::string _tmp = _r.readString(); _s.label = strdup(_tmp.c_str()); }
    // bytes: unsupported field type Uint8List in inline codec
    // origin: nested struct field not supported in inline codec
    // status: unsupported field type TcStatus in inline codec
    _s.ok = _r.readBool() ? 1 : 0;
    _s.count = _r.readInt();
    return _s;
}
inline void nitro_TcRichStruct_encodeInto(const TcRichStruct& _s, NitroRecordWriter& w) {
    w.writeString(_s.label ? std::string(_s.label) : std::string());
    // bytes: unsupported field type Uint8List in inline codec
    // origin: unsupported field type TcPoint in inline codec
    // status: unsupported field type TcStatus in inline codec
    w.writeBool(_s.ok != 0);
    w.writeInt(_s.count);
}

// @HybridRecord C++ structs (generated by Nitrogen)
struct TcConfig;
struct TcMeta;
struct TcPacket;
struct TcNested;
struct NitroNullableInt;
struct NitroNullableDouble;
struct TcNullableWrapper;
struct TcDataRecord;
struct TcStructHolder;
struct TcDeepRecord;
struct TcPair;
struct NitroNullableBool;

struct TcConfig {
    std::string name;
    int64_t count;
    bool enabled;
    double threshold;

    static TcConfig fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static TcConfig fromReader(NitroRecordReader& _r) {
        TcConfig _obj;
        _obj.name = _r.readString();
        _obj.count = _r.readInt();
        _obj.enabled = _r.readBool();
        _obj.threshold = _r.readDouble();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeString(name);
        w.writeInt(count);
        w.writeBool(enabled);
        w.writeDouble(threshold);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct TcMeta {
    int64_t version;
    double weight;
    bool active;
    std::string label;

    static TcMeta fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static TcMeta fromReader(NitroRecordReader& _r) {
        TcMeta _obj;
        _obj.version = _r.readInt();
        _obj.weight = _r.readDouble();
        _obj.active = _r.readBool();
        _obj.label = _r.readString();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeInt(version);
        w.writeDouble(weight);
        w.writeBool(active);
        w.writeString(label);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct TcPacket {
    std::string name;
    int64_t sequence;
    TcStatus status;
    bool valid;

    static TcPacket fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static TcPacket fromReader(NitroRecordReader& _r) {
        TcPacket _obj;
        _obj.name = _r.readString();
        _obj.sequence = _r.readInt();
        _obj.status = static_cast<TcStatus>(_r.readInt());
        _obj.valid = _r.readBool();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeString(name);
        w.writeInt(sequence);
        w.writeInt(static_cast<int64_t>(status));
        w.writeBool(valid);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct TcNested {
    std::string label;
    TcConfig config;
    int64_t version;

    static TcNested fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static TcNested fromReader(NitroRecordReader& _r) {
        TcNested _obj;
        _obj.label = _r.readString();
        _obj.config = TcConfig::fromReader(_r);
        _obj.version = _r.readInt();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeString(label);
        config.encodeInto(w);
        w.writeInt(version);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct NitroNullableInt {
    bool hasValue;
    int64_t value;

    static NitroNullableInt fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static NitroNullableInt fromReader(NitroRecordReader& _r) {
        NitroNullableInt _obj;
        _obj.hasValue = _r.readBool();
        _obj.value = _r.readInt();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeBool(hasValue);
        w.writeInt(value);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct NitroNullableDouble {
    bool hasValue;
    double value;

    static NitroNullableDouble fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static NitroNullableDouble fromReader(NitroRecordReader& _r) {
        NitroNullableDouble _obj;
        _obj.hasValue = _r.readBool();
        _obj.value = _r.readDouble();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeBool(hasValue);
        w.writeDouble(value);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct TcNullableWrapper {
    NitroNullableInt count;
    NitroNullableDouble rate;
    std::string name;

    static TcNullableWrapper fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static TcNullableWrapper fromReader(NitroRecordReader& _r) {
        TcNullableWrapper _obj;
        _obj.count = NitroNullableInt::fromReader(_r);
        _obj.rate = NitroNullableDouble::fromReader(_r);
        _obj.name = _r.readString();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        count.encodeInto(w);
        rate.encodeInto(w);
        w.writeString(name);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct TcDataRecord {
    std::vector<uint8_t> bytes;
    std::vector<uint8_t> values;
    std::vector<uint8_t> scores;
    std::string label;

    static TcDataRecord fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static TcDataRecord fromReader(NitroRecordReader& _r) {
        TcDataRecord _obj;
        { int32_t _n = _r.readInt32(); _obj.bytes.resize((size_t)_n); _r.readBytes(_obj.bytes.data(), (size_t)_n); }
        { int32_t _n = _r.readInt32(); _obj.values.resize((size_t)_n); _r.readBytes(_obj.values.data(), (size_t)_n); }
        { int32_t _n = _r.readInt32(); _obj.scores.resize((size_t)_n); _r.readBytes(_obj.scores.data(), (size_t)_n); }
        _obj.label = _r.readString();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        { w.writeInt32((int32_t)bytes.size()); w.writeBytes(bytes.data(), bytes.size()); }
        { w.writeInt32((int32_t)values.size()); w.writeBytes(values.data(), values.size()); }
        { w.writeInt32((int32_t)scores.size()); w.writeBytes(scores.data(), scores.size()); }
        w.writeString(label);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct TcStructHolder {
    std::string label;
    TcPoint origin;
    double radius;

    static TcStructHolder fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static TcStructHolder fromReader(NitroRecordReader& _r) {
        TcStructHolder _obj;
        _obj.label = _r.readString();
        _obj.origin = nitro_TcPoint_fromReader(_r);
        _obj.radius = _r.readDouble();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeString(label);
        nitro_TcPoint_encodeInto(origin, w);
        w.writeDouble(radius);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct TcDeepRecord {
    std::string label;
    TcNested nested;
    int64_t depth;

    static TcDeepRecord fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static TcDeepRecord fromReader(NitroRecordReader& _r) {
        TcDeepRecord _obj;
        _obj.label = _r.readString();
        _obj.nested = TcNested::fromReader(_r);
        _obj.depth = _r.readInt();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeString(label);
        nested.encodeInto(w);
        w.writeInt(depth);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct TcPair {
    int64_t field0;
    std::string field1;

    static TcPair fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static TcPair fromReader(NitroRecordReader& _r) {
        TcPair _obj;
        _obj.field0 = _r.readInt();
        _obj.field1 = _r.readString();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeInt(field0);
        w.writeString(field1);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct NitroNullableBool {
    bool hasValue;
    bool value;

    static NitroNullableBool fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static NitroNullableBool fromReader(NitroRecordReader& _r) {
        NitroNullableBool _obj;
        _obj.hasValue = _r.readBool();
        _obj.value = _r.readBool();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeBool(hasValue);
        w.writeBool(value);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

// @NitroVariant C++ structs and codecs (generated by Nitrogen)
// Dart variant fields encode enums by case index (enum.index), not nativeValue.
inline TcStatus nitro_TcStatus_fromIndex(int64_t i) {
    static const int64_t _v[3] = { 0, 1, 2 };
    return static_cast<TcStatus>((i >= 0 && i < 3) ? _v[i] : _v[0]);
}
inline int64_t nitro_TcStatus_toIndex(TcStatus x) {
    static const int64_t _v[3] = { 0, 1, 2 };
    for (int64_t i = 0; i < 3; i++) { if (_v[i] == (int64_t)x) return i; }
    return 0;
}

struct TcEventTap {
    int64_t x;
    int64_t y;
};
struct TcEventScroll {
    double delta;
};
struct TcEventResize {
    int64_t width;
    int64_t height;
};
struct TcEventNullable {
    std::optional<int64_t> count;
    std::optional<TcStatus> status;
    std::optional<TcConfig> config;
    std::optional<std::vector<int64_t>> samples;
};

using TcEvent = std::variant<TcEventTap, TcEventScroll, TcEventResize, TcEventNullable>;

/// Decodes a TcEvent from a wire payload view (no 4-byte length prefix).
inline TcEvent nitro_decode_TcEvent(NitroCppBuffer buf) {
    NitroRecordReader _r(buf);
    int8_t tag = _r.readInt8();
    switch (tag) {
    case 0: {
        TcEventTap _c{};
        _c.x = _r.readInt();
        _c.y = _r.readInt();
        return _c;
    }
    case 1: {
        TcEventScroll _c{};
        _c.delta = _r.readDouble();
        return _c;
    }
    case 2: {
        TcEventResize _c{};
        _c.width = _r.readInt();
        _c.height = _r.readInt();
        return _c;
    }
    case 3: {
        TcEventNullable _c{};
        if (_r.readBool()) { _c.count = _r.readInt(); }
        if (_r.readBool()) { _c.status = nitro_TcStatus_fromIndex(_r.readInt()); }
        if (_r.readBool()) { _c.config = TcConfig::fromReader(_r); }
        if (_r.readBool()) { int32_t _n = _r.readInt32(); auto& _vec = _c.samples.emplace(); _vec.reserve((size_t)_n); for (int32_t _i = 0; _i < _n; _i++) _vec.push_back(_r.readInt()); }
        return _c;
    }
    default: throw std::runtime_error("TcEvent: unknown tag");
    }
}

/// Appends the wire payload (tag + fields, no length prefix) to [w].
inline void nitro_encode_TcEvent(const TcEvent& v, NitroRecordWriter& w) {
    std::visit([&w](auto&& c) {
        using T = std::decay_t<decltype(c)>;
        (void)c;
        if constexpr (std::is_same_v<T, TcEventTap>) {
            w.writeInt8(0); // tag
            w.writeInt(c.x);
            w.writeInt(c.y);
        }
        if constexpr (std::is_same_v<T, TcEventScroll>) {
            w.writeInt8(1); // tag
            w.writeDouble(c.delta);
        }
        if constexpr (std::is_same_v<T, TcEventResize>) {
            w.writeInt8(2); // tag
            w.writeInt(c.width);
            w.writeInt(c.height);
        }
        if constexpr (std::is_same_v<T, TcEventNullable>) {
            w.writeInt8(3); // tag
            w.writeBool(c.count.has_value());
            if (c.count.has_value()) { w.writeInt((*c.count)); }
            w.writeBool(c.status.has_value());
            if (c.status.has_value()) { w.writeInt(nitro_TcStatus_toIndex((*c.status))); }
            w.writeBool(c.config.has_value());
            if (c.config.has_value()) { (*c.config).encodeInto(w); }
            w.writeBool(c.samples.has_value());
            if (c.samples.has_value()) { { w.writeInt32((int32_t)(*c.samples).size()); for (const auto& _e : (*c.samples)) { w.writeInt(_e); } } }
        }
    }, v);
}

/// Heap-allocated [4B length][payload] block for returning a TcEvent
/// to Dart (method returns / property getters). Ownership transfers to Dart.
inline NitroCppBuffer nitro_TcEvent_to_native(const TcEvent& v) {
    NitroRecordWriter _w;
    nitro_encode_TcEvent(v, _w);
    return _w.toNativeBuffer();
}

/// Abstract C++ interface for NitroTypeCoverage.
/// 1. Subclass this and implement all pure-virtual methods.
/// 2. Call nitro_type_coverage_register_impl(&myInstance) at startup.
/// 3. Call nitro_type_coverage_register_impl(nullptr) in teardown.
class HybridNitroTypeCoverage {
public:
    virtual ~HybridNitroTypeCoverage() = default;

    // ── Methods ──────────────────────────────────────────────────────────
    // source: nitro_type_coverage.native.dart:62
    virtual int64_t echoInt(int64_t value) = 0;
    // source: nitro_type_coverage.native.dart:63
    virtual double echoDouble(double value) = 0;
    // source: nitro_type_coverage.native.dart:64
    virtual bool echoBool(bool value) = 0;
    // source: nitro_type_coverage.native.dart:65
    virtual std::string echoString(const std::string& value) = 0;
    // source: nitro_type_coverage.native.dart:68
    virtual int64_t addInts(int64_t a, int64_t b, int64_t c) = 0;
    // source: nitro_type_coverage.native.dart:69
    virtual double mulDoubles(double a, double b) = 0;
    // source: nitro_type_coverage.native.dart:70
    virtual std::string joinStrings(const std::string& a, const std::string& b, const std::string& separator) = 0;
    // source: nitro_type_coverage.native.dart:73
    virtual int64_t echoDateTime(int64_t value) = 0;
    // source: nitro_type_coverage.native.dart:74
    virtual std::optional<int64_t> echoNullableDateTime(std::optional<int64_t> value) = 0;
    // source: nitro_type_coverage.native.dart:77
    virtual std::optional<int64_t> echoNullableInt(std::optional<int64_t> value) = 0;
    // source: nitro_type_coverage.native.dart:78
    virtual std::optional<double> echoNullableDouble(std::optional<double> value) = 0;
    // source: nitro_type_coverage.native.dart:79
    virtual std::optional<bool> echoNullableBool(std::optional<bool> value) = 0;
    // source: nitro_type_coverage.native.dart:80
    virtual std::optional<std::string> echoNullableString(const std::optional<std::string>& value) = 0;
    // source: nitro_type_coverage.native.dart:83
    virtual TcStatus echoStatus(TcStatus value) = 0;
    // source: nitro_type_coverage.native.dart:84
    virtual std::optional<TcStatus> echoNullableStatus(std::optional<TcStatus> value) = 0;
    // source: nitro_type_coverage.native.dart:87
    virtual TcPoint echoPoint(const TcPoint& value) = 0;
    // source: nitro_type_coverage.native.dart:90
    virtual TcRichStruct echoRichStruct(const TcRichStruct& value) = 0;
    // source: nitro_type_coverage.native.dart:93
    virtual NitroCppBuffer echoConfig(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:97
    virtual NitroCppBuffer echoBytes(const uint8_t* value, size_t value_length) = 0;
    // source: nitro_type_coverage.native.dart:100
    virtual NitroCppBuffer echoFloats(const float* value, size_t value_length) = 0;
    // source: nitro_type_coverage.native.dart:103
    virtual NitroCppBuffer echoFloat64s(const double* value, size_t value_length) = 0;
    // source: nitro_type_coverage.native.dart:106
    virtual NitroCppBuffer echoInt32s(const int32_t* value, size_t value_length) = 0;
    // source: nitro_type_coverage.native.dart:110
    virtual NitroCppBuffer echoInt8s(const int8_t* value, size_t value_length) = 0;
    // source: nitro_type_coverage.native.dart:113
    virtual NitroCppBuffer echoInt16s(const int16_t* value, size_t value_length) = 0;
    // source: nitro_type_coverage.native.dart:116
    virtual NitroCppBuffer echoInt64s(const int64_t* value, size_t value_length) = 0;
    // source: nitro_type_coverage.native.dart:120
    virtual NitroCppBuffer echoIntList(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:123
    virtual NitroCppBuffer echoDoubleList(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:126
    virtual NitroCppBuffer echoStringList(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:129
    virtual NitroCppBuffer echoConfigList(NitroCppBuffer values) = 0;
    // source: nitro_type_coverage.native.dart:133
    virtual int64_t asyncInt(int64_t value) = 0;
    // source: nitro_type_coverage.native.dart:136
    virtual double asyncDouble(double value) = 0;
    // source: nitro_type_coverage.native.dart:139
    virtual bool asyncBool(bool value) = 0;
    // source: nitro_type_coverage.native.dart:142
    virtual std::string asyncString(const std::string& value) = 0;
    // source: nitro_type_coverage.native.dart:145
    virtual NitroCppBuffer asyncConfig(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:149
    virtual std::optional<int64_t> asyncNullableInt(std::optional<int64_t> value) = 0;
    // source: nitro_type_coverage.native.dart:152
    virtual std::optional<double> asyncNullableDouble(std::optional<double> value) = 0;
    // source: nitro_type_coverage.native.dart:155
    virtual std::optional<bool> asyncNullableBool(std::optional<bool> value) = 0;
    // source: nitro_type_coverage.native.dart:158
    virtual std::optional<std::string> asyncNullableString(const std::optional<std::string>& value) = 0;
    // source: nitro_type_coverage.native.dart:162
    virtual TcPoint asyncPoint(const TcPoint& value) = 0;
    // source: nitro_type_coverage.native.dart:165
    virtual std::optional<TcStatus> asyncNullableStatus(std::optional<TcStatus> value) = 0;
    // source: nitro_type_coverage.native.dart:168
    virtual NitroCppBuffer asyncMeta(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:171
    virtual NitroCppBuffer echoMeta(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:174
    virtual NitroCppBuffer echoNullableIntSafe(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:175
    virtual NitroCppBuffer echoNullableDoubleSafe(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:176
    virtual NitroCppBuffer echoNullableBoolSafe(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:180
    virtual NitroCppBuffer echoDataRecord(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:186
    virtual NitroCppBuffer echoIntMap(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:187
    virtual NitroCppBuffer echoStringMap(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:188
    virtual NitroCppBuffer echoDoubleMap(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:189
    virtual NitroCppBuffer echoBoolMap(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:191
    virtual NitroCppBuffer echoConfigMap(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:192
    virtual NitroCppBuffer echoEventMap(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:196
    virtual TcOptScalars echoOptScalars(const TcOptScalars& value) = 0;
    // source: nitro_type_coverage.native.dart:197
    virtual NitroCppBuffer echoNullableIntMap(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:198
    virtual NitroCppBuffer echoNullableDoubleMap(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:199
    virtual NitroCppBuffer echoNullableBoolMap(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:200
    virtual NitroCppBuffer echoNullableStringMap(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:204
    virtual NitroCppBuffer echoPacket(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:208
    virtual std::optional<TcPoint> echoNullablePoint(const std::optional<TcPoint>& value) = 0;
    // source: nitro_type_coverage.native.dart:214
    virtual void configureConfigStream(NitroCppBuffer seed, int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:218
    virtual NitroCppBuffer echoNullableConfig(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:221
    virtual NitroCppBuffer echoNested(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:226
    virtual NitroCppBuffer echoConfigListSync(NitroCppBuffer values) = 0;
    // source: nitro_type_coverage.native.dart:229
    virtual NitroCppBuffer echoNullableWrapper(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:233
    virtual void onTransformEvent(std::function<int64_t(int64_t)> transformCb) = 0;
    // source: nitro_type_coverage.native.dart:236
    virtual NitroCppBuffer echoStructHolder(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:239
    virtual void onStringTransform(std::function<std::string(int64_t)> stringCb) = 0;
    // source: nitro_type_coverage.native.dart:240
    virtual void onDoubleTransform(std::function<double(int64_t)> doubleCb) = 0;
    // source: nitro_type_coverage.native.dart:245
    virtual void configureBatchStream(int64_t from, int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:249
    virtual void configureBatchDoubleStream(NitroCppBuffer values) = 0;
    // source: nitro_type_coverage.native.dart:253
    virtual void configureBatchBoolStream(NitroCppBuffer values) = 0;
    // source: nitro_type_coverage.native.dart:256
    virtual void onBoolTransform(std::function<bool(int64_t)> boolCb) = 0;
    // source: nitro_type_coverage.native.dart:257
    virtual void onStatusTransform(std::function<TcStatus(int64_t)> statusCb) = 0;
    // source: nitro_type_coverage.native.dart:261
    virtual NitroCppBuffer echoListBool(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:264
    virtual NitroCppBuffer echoPointList(NitroCppBuffer values) = 0;
    // source: nitro_type_coverage.native.dart:268
    virtual void submitCoalesced(int64_t callId, int64_t value, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:272
    virtual void nativeAsyncInt(int64_t value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:275
    virtual void nativeAsyncDouble(double value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:278
    virtual void nativeAsyncBool(bool value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:281
    virtual void nativeAsyncString(const std::string& value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:285
    virtual void nativeAsyncNullableInt(std::optional<int64_t> value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:288
    virtual void nativeAsyncNullableDouble(std::optional<double> value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:291
    virtual void nativeAsyncNullableBool(std::optional<bool> value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:302
    virtual void nativeAsyncStatus(TcStatus value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:305
    virtual void nativeAsyncNullableStatus(std::optional<TcStatus> value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:308
    virtual void nativeAsyncConfig(NitroCppBuffer value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:314
    virtual void nativeAsyncPrintText(const std::string& text, NitroCppBuffer settings, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:321
    virtual std::string implVariant() = 0;
    // source: nitro_type_coverage.native.dart:324
    virtual void nativeAsyncNullableConfig(NitroCppBuffer value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:327
    virtual void nativeAsyncEvent(NitroCppBuffer value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:330
    virtual void nativeAsyncConfigList(NitroCppBuffer values, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:333
    virtual void nativeAsyncStatusList(NitroCppBuffer values, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:336
    virtual void nativeAsyncEventList(NitroCppBuffer values, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:339
    virtual void nativeAsyncIntList(NitroCppBuffer values, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:342
    virtual void nativeAsyncWithCallback(int64_t value, std::function<void(int64_t)> callback, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:345
    virtual void nativeAsyncCounts(int64_t seed, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:348
    virtual void nativeAsyncNullableUint64(std::optional<uint64_t> value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:357
    virtual void nativeAsyncEchoIntMap(NitroCppBuffer value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:360
    virtual void nativeAsyncEchoPoint(const TcPoint& value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:363
    virtual void nativeAsyncEchoAnyMap(void* value, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:368
    virtual void configureStringStream(NitroCppBuffer values) = 0;
    // source: nitro_type_coverage.native.dart:373
    virtual void configureBatchStringStream(NitroCppBuffer values) = 0;
    // source: nitro_type_coverage.native.dart:378
    virtual void configureBlockIntStream(int64_t from, int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:381
    virtual void onPointEvent(std::function<void(const TcPoint&)> pointCb) = 0;
    // source: nitro_type_coverage.native.dart:382
    virtual void onDetailEvent(std::function<void(int64_t, double)> detailCb) = 0;
    // source: nitro_type_coverage.native.dart:385
    virtual void onIntEvent(std::function<void(int64_t)> callback) = 0;
    // source: nitro_type_coverage.native.dart:391
    virtual void onBoolEvent(std::function<void(bool)> boolCb) = 0;
    // source: nitro_type_coverage.native.dart:392
    virtual void onDoubleEvent(std::function<void(double)> doubleCb) = 0;
    // source: nitro_type_coverage.native.dart:434
    virtual void configureStream(int64_t from, int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:435
    virtual void configureDoubleStream(double start, int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:436
    virtual void configureStatusStream(int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:439
    virtual void throwNative(const std::string& message) = 0;
    // source: nitro_type_coverage.native.dart:442
    virtual void throwNativeAsync(const std::string& message) = 0;
    // source: nitro_type_coverage.native.dart:449
    virtual void throwNativeNativeAsync(const std::string& message, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:461
    virtual NitroCppBuffer getConfigOrFail(bool shouldFail) = 0;
    // source: nitro_type_coverage.native.dart:469
    virtual void nativeAsyncEchoOptionalConfig(NitroCppBuffer config, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_type_coverage.native.dart:474
    virtual void* acquireBuffer(int64_t size) = 0;
    // source: nitro_type_coverage.native.dart:478
    virtual NitroCppBuffer echoEvent(NitroCppBuffer event) = 0;
    // source: nitro_type_coverage.native.dart:483
    virtual double safeDiv(double a, double b) = 0;
    // source: nitro_type_coverage.native.dart:488
    virtual std::string validateLabel(const std::string& label) = 0;
    // source: nitro_type_coverage.native.dart:494
    virtual int64_t slowAsync(int64_t delayMs) = 0;
    // source: nitro_type_coverage.native.dart:498
    virtual NitroCppBuffer echoDeepRecord(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:501
    virtual NitroCppBuffer asyncDeepRecord(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:507
    virtual void* asyncAcquireBuffer(int64_t size) = 0;
    // source: nitro_type_coverage.native.dart:511
    virtual NitroCppBuffer asyncEchoEvent(NitroCppBuffer event) = 0;
    // source: nitro_type_coverage.native.dart:516
    virtual double asyncSafeDiv(double a, double b) = 0;
    // source: nitro_type_coverage.native.dart:521
    virtual std::string asyncValidateLabel(const std::string& label) = 0;
    // source: nitro_type_coverage.native.dart:524
    virtual TcPriority echoPriority(TcPriority value) = 0;
    // source: nitro_type_coverage.native.dart:529
    virtual void configureBufferDropIntStream(int64_t from, int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:532
    virtual void onEventCallback(std::function<void(NitroCppBuffer)> handler) = 0;
    // source: nitro_type_coverage.native.dart:537
    virtual void configureEventStream(int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:540
    virtual NitroCppBuffer getStatusList() = 0;
    // source: nitro_type_coverage.native.dart:541
    virtual NitroCppBuffer echoStatusList(NitroCppBuffer values) = 0;
    // source: nitro_type_coverage.native.dart:544
    virtual NitroCppBuffer getEventList() = 0;
    // source: nitro_type_coverage.native.dart:545
    virtual NitroCppBuffer echoEventList(NitroCppBuffer values) = 0;
    // source: nitro_type_coverage.native.dart:554
    virtual void configureNullableStatusStream(int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:558
    virtual void configureNullableStringStream(int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:564
    virtual NitroCppBuffer echoPair(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:565
    virtual NitroCppBuffer echoNullablePair(NitroCppBuffer value) = 0;
    // source: nitro_type_coverage.native.dart:570
    virtual uint64_t echoUint64(uint64_t value) = 0;
    // source: nitro_type_coverage.native.dart:571
    virtual std::optional<uint64_t> echoNullableUint64(std::optional<uint64_t> value) = 0;
    // source: nitro_type_coverage.native.dart:576
    virtual void configureUint64Stream(int64_t from, int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:580
    virtual void configureNullableUint64Stream(int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:583
    virtual int64_t echoInt8(int64_t value) = 0;
    // source: nitro_type_coverage.native.dart:584
    virtual int64_t echoInt16(int64_t value) = 0;
    // source: nitro_type_coverage.native.dart:585
    virtual int64_t echoInt32(int64_t value) = 0;
    // source: nitro_type_coverage.native.dart:586
    virtual int64_t echoUint8(int64_t value) = 0;
    // source: nitro_type_coverage.native.dart:587
    virtual int64_t echoUint16(int64_t value) = 0;
    // source: nitro_type_coverage.native.dart:588
    virtual int64_t echoUint32(int64_t value) = 0;
    // source: nitro_type_coverage.native.dart:589
    virtual double echoFloat(double value) = 0;
    // source: nitro_type_coverage.native.dart:590
    virtual std::optional<int64_t> echoNullableInt32(std::optional<int64_t> value) = 0;
    // source: nitro_type_coverage.native.dart:591
    virtual std::optional<double> echoNullableFloat(std::optional<double> value) = 0;
    // source: nitro_type_coverage.native.dart:596
    virtual void configureNullableIntStream(int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:600
    virtual void configureNullableDoubleStream(int64_t count) = 0;
    // source: nitro_type_coverage.native.dart:604
    virtual void configureNullableBoolStream(int64_t count) = 0;

    // ── Properties ───────────────────────────────────────────────────────
    virtual int64_t get_precision() const = 0;
    virtual void set_precision(int64_t value) = 0;
    virtual std::string get_tag() const = 0;
    virtual void set_tag(const std::string& value) = 0;
    virtual std::optional<double> get_nullableRate() const = 0;
    virtual void set_nullableRate(std::optional<double> value) = 0;
    virtual bool get_enabled() const = 0;
    virtual void set_enabled(bool value) = 0;
    virtual TcStatus get_currentStatus() const = 0;
    virtual void set_currentStatus(TcStatus value) = 0;
    virtual std::optional<int64_t> get_nullableCounter() const = 0;
    virtual void set_nullableCounter(std::optional<int64_t> value) = 0;
    virtual std::optional<bool> get_optionalFlag() const = 0;
    virtual void set_optionalFlag(std::optional<bool> value) = 0;
    virtual NitroCppBuffer get_currentEvent() const = 0;
    virtual void set_currentEvent(NitroCppBuffer value) = 0;

    // ── Streams ──────────────────────────────────────────────────────────
    // Call the emit_* helpers below to push items to Dart from any thread.
    /// Emit a value on the configStream stream.
    /// Pass record.toNativeBuffer() (or nitro_Xxx_to_native(...)) — a heap
    /// [4B len][payload] block. Ownership transfers: the bridge/Dart frees
    /// it. Never pass a non-owning writer.toBuffer() view.
    void emit_configStream(NitroCppBuffer item);
    /// Emit a value on the batchIntStream stream.
    void emit_batchIntStream(int64_t item);
    /// Emit a value on the batchDoubleStream stream.
    void emit_batchDoubleStream(double item);
    /// Emit a value on the batchBoolStream stream.
    void emit_batchBoolStream(bool item);
    /// Emit a value on the stringStream stream.
    void emit_stringStream(std::string item);
    /// Emit a value on the batchStringStream stream.
    void emit_batchStringStream(std::string item);
    /// Emit a value on the blockIntStream stream.
    void emit_blockIntStream(int64_t item);
    /// Emit a value on the intStream stream.
    void emit_intStream(int64_t item);
    /// Emit a value on the pointStream stream.
    void emit_pointStream(TcPoint item);
    /// Emit a value on the boolStream stream.
    void emit_boolStream(bool item);
    /// Emit a value on the doubleStream stream.
    void emit_doubleStream(double item);
    /// Emit a value on the statusStream stream.
    void emit_statusStream(TcStatus item);
    /// Emit a value on the bufferDropIntStream stream.
    void emit_bufferDropIntStream(int64_t item);
    /// Emit a value on the eventStream stream.
    /// Pass record.toNativeBuffer() (or nitro_Xxx_to_native(...)) — a heap
    /// [4B len][payload] block. Ownership transfers: the bridge/Dart frees
    /// it. Never pass a non-owning writer.toBuffer() view.
    void emit_eventStream(NitroCppBuffer item);
    /// Emit a value on the nullableStatusStream stream.
    void emit_nullableStatusStream(std::optional<TcStatus> item);
    /// Emit a value on the nullableStringStream stream.
    void emit_nullableStringStream(std::optional<std::string> item);
    /// Emit a value on the uint64Stream stream.
    void emit_uint64Stream(uint64_t item);
    /// Emit a value on the nullableUint64Stream stream.
    void emit_nullableUint64Stream(std::optional<uint64_t> item);
    /// Emit a value on the nullableIntStream stream.
    void emit_nullableIntStream(std::optional<int64_t> item);
    /// Emit a value on the nullableDoubleStream stream.
    void emit_nullableDoubleStream(std::optional<double> item);
    /// Emit a value on the nullableBoolStream stream.
    void emit_nullableBoolStream(std::optional<bool> item);

protected:
    HybridNitroTypeCoverage() = default;
};

#ifdef __cplusplus
/// Factory function type — mirrors RN Nitro's HybridObjectRegistry constructor fn.
/// Takes the instance key (from Dart's getInstance(key)) and returns a shared_ptr.
using HybridNitroTypeCoverageFactory = std::function<std::shared_ptr<HybridNitroTypeCoverage>(const std::string&)>;
#endif

#ifdef __cplusplus
extern "C" {
#endif

/// Register a factory function (recommended — enables multiple instances).
/// Called from create_instance() each time Dart requests a new object.
void nitro_type_coverage_register_factory(void* factory_fn_ptr);

/// Register a single raw-pointer implementation (legacy single-instance API).
/// Use register_factory() instead for new code.
void nitro_type_coverage_register_impl(HybridNitroTypeCoverage* impl);

/// Return the registered raw-pointer impl, or nullptr (legacy API).
HybridNitroTypeCoverage* nitro_type_coverage_get_impl(void);

#ifdef __cplusplus
} // end extern "C"

/// C++-only typed factory registration — preferred over the C void* variant.
inline void nitro_type_coverage_register_factory_typed(HybridNitroTypeCoverageFactory factory) {
    nitro_type_coverage_register_factory(static_cast<void*>(&factory));
}
#endif

#endif // NITRO_TYPE_COVERAGE_NATIVE_G_H
